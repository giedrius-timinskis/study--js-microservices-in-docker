sudo: required
services:
  - docker

# We want to make sure tests are passing before running the production build
before_install:
  # Build docker image to use for running tests
  - docker build -t gtntaz/run-client-tests -f ./client/Dockerfile.dev ./client

script:
  # Script for running tests in /client
  - docker run gtntaz/run-client-tests npm run test:ci

# Tests passed, run production build
after_success:
  # Build all Docker images using default Dockerfile config
  - docker build -t gtntaz/study--js-microservices-in-docker-client ./client
  - docker build -t gtntaz/study--js-microservices-in-docker-nginx ./nginx
  - docker build -t gtntaz/study--js-microservices-in-docker-server ./server
  - docker build -t gtntaz/study--js-microservices-in-docker-worker ./worker
  # Log in to Docker CLI
  # First execute righthand side - login and enter account name, then use the lefthand as input for password 
  - echo "$DOCKER_PWD" | docker login -u "$DOCKER_ID" --password-stdin
  # Push images to Docker Hub
  - docker push gtntaz/study--js-microservices-in-docker-client
  - docker push gtntaz/study--js-microservices-in-docker-nginx
  - docker push gtntaz/study--js-microservices-in-docker-server
  - docker push gtntaz/study--js-microservices-in-docker-worker